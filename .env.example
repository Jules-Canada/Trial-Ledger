# Copy to .env.local (gitignored) and fill in, OR leave unset and provide the
# key from your shell / macOS Keychain at runtime. Used ONLY by the offline
# authoring CLI (pipeline/), never by the browser app. Never prefix with VITE_.
ANTHROPIC_API_KEY=
