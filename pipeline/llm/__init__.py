"""LLM provider layer. The authoring orchestrator depends only on the
vendor-neutral `LlmProvider` base class (provider.py); Claude is the first
implementation (claude.py). See docs/design/authoring-pipeline.md.
"""
